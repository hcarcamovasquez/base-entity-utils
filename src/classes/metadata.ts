export interface Metadata {
    readonly createdAt: string;
    readonly updatedAt: string;
}

export interface BaseUuidIdentification {
    readonly uuid: string;
}

export interface BaseIdIdentification {
    readonly id: string;
}
